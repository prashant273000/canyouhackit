// Phase 1 only: establish a predictable marker for supported pages.
// Content parsing and moderation are intentionally deferred to later phases.
document.documentElement.dataset.aiFeedShield = "ready";
